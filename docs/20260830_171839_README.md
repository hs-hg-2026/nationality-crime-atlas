# Collection and Visualization of Crime Statistics and Resident Population by Prefecture and Nationality

Verify trustworthy data sources; regularly collect nationwide crime statistics and resident populations by nationality, prefecture, and region; and publish counts plus reproducible public-data-derived reference ratios on GitHub.

日本語版: [README.ja.md](./README.ja.md)

## Background
Public data on resident populations and crime statistics are fragmented across publishers and may not share the same geographic, temporal, or nationality definitions. The detailed framing and cautions are maintained in [docs/brief.md](./docs/brief.md).

## Goals / Milestones
1. Audit primary sources and define comparable metrics.
2. Build a provenance-preserving collection and normalization pipeline.
3. Publish counts and source-pair-specific reference ratios, always showing their numerator, denominator, formula, provenance, and mismatches.

## Approach
Treat each numerator/denominator pairing as a separately named indicator. Preserve both official definitions, calculate only observable combinations, expose mismatches, and never present a project-derived ratio as an official or exact crime rate.

## Status
M0 and M1 are complete. The parser core of M2 was implemented and verified on 2026-08-30: three official-workbook parsers, normalized records, a source registry, and JSONL plus provenance-manifest output are working. M2 remains in progress until automated acquisition, immutable raw snapshots, and full-dataset quality gates are added.

The current test suite has 17 passing tests with 88.06% total coverage (branch measurement enabled). Real-source smoke checks parsed 468,641 population rows, 33 Table 130 rows, 30 Table 131 rows, and 360 Table 13 records. Exact numerator–denominator alignment is not a milestone, and unavailable `nationality × prefecture` numerators will not be imputed.

## Components
- Versioned official-source metadata: [config/sources.json](./config/sources.json)
- Source parsers and normalized models: [src/nationality_crime_atlas](./src/nationality_crime_atlas)
- Parser and provenance tests: [tests](./tests)
- Living scientific and product through-line: [docs/brief.md](./docs/brief.md)
- Phase map and current position: [docs/workflow.md](./docs/workflow.md)
- Decisions and troubleshooting highlights: [logbook.md](./logbook.md)
- Focused parallel-research prompt: [docs/20260830_135535_parallel_deep_research_prompt.md](./docs/20260830_135535_parallel_deep_research_prompt.md)

## Usage
Create a project-local environment and install the package with development dependencies:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -e '.[dev]'
```

Run the verified quality gate and inspect the ingestion commands:

```bash
.venv/bin/python -m pytest --cov=nationality_crime_atlas --cov-report=term-missing
.venv/bin/nca-ingest --help
```

The CLI accepts a previously acquired official XLS/XLSX artifact and writes normalized JSONL plus a provenance manifest. Automated download and placement under `data/raw/` have not been implemented yet.
