# Nationality Crime Atlas for Japan

This project collects official Japanese crime and population statistics, preserves their definitions and provenance, and visualizes counts plus reproducible public-data-derived reference ratios without treating nationality as an inherent risk trait.

[日本語](./README.ja.md)

## Background

Crime numerators and population denominators are published by different agencies and often differ in geography, date, population scope, and nationality grouping. Showing nationality labels without that context can encourage misleading impressions. This project therefore keeps the concrete public values visible, makes incompatible or unavailable combinations explicit, and leaves causal or moral judgments outside the visualization.

## Goals / Milestones

1. Inventory and verify primary official sources.
2. Acquire immutable raw artifacts and build reproducible normalized data products.
3. Publish counts, formulas, sources, mismatch flags, warnings, and refusals together.
4. Visualize an all-resident regional context and a separate nationwide nationality comparison.
5. Add historical editions, year-over-year stability checks, and scheduled updates.

## Approach

- The primary regional view uses the total population residing in Japan, including Japanese and foreign residents.
- The secondary nationwide comparison includes every published/evaluable nationality category, including Japanese nationality. It shows the five higher and five lower observed ratios symmetrically and also exposes the complete 26-row table.
- The Japanese numerator is a project-derived residual: all-person criminal-code cleared persons minus all-foreign criminal-code cleared persons. It is labeled as a reference estimate, never as a directly published Japanese value.
- Regional context is displayed per 100,000 total residents; nationwide nationality comparison is displayed per 1,000 corresponding residents. Raw numerators and denominators remain available.
- Every computed value is called a `public-data-derived reference ratio`, not an official or exact crime rate.
- Unpublished `individual nationality × prefecture` crime numerators are not estimated or allocated.
- Small denominators and sparse numerators remain visible with warnings; they are not silently removed.

See [docs/interpretation_note.md](./docs/interpretation_note.md) for the permanent interpretation policy.

## Status

**Current phase: M4 visualization and its fresh independent review are complete. Restoring the nationwide numerator selector and verifying the live deployment are the remaining release gates.**

- Registry: 7 source series / 8 immutable editions, all present in the validated artifact catalog.
- All-resident regional context: 186 rows — 144 calculated, 42 explicitly refused.
- Nationwide nationality comparison: 26 rows — 22 calculated, 4 explicitly refused.
- Supplementary nationality indicators: 290 rows — 250 calculated, 40 explicitly refused.
- Dashboard bundle: compact-export schema v3, 8 sanitized sources, SHA-256 `b0f950baa23aac85e44d644b4609ec0bd70d0567d307bfaf013377d85e4ee060`.
- Japanese reference row: numerator 181,362; denominator 120,296,000; reference ratio 1.5076 per 1,000. Numerator sources are S15 − S08; denominator source is S17.
- Local dashboard: desktop and 390 px mobile layouts inspected; all 26 rows, four refusals, source IDs, and warnings are visible.
- Verification: Python 116 tests passed with no skips and 84.12% coverage. Web 56 tests passed with 80.41% branch coverage; typecheck, lint, format, data-hash verification, production build, and the final 32-file GitHub Pages artifact verification passed.

The GitHub Pages workflow exists under `.github/workflows/pages.yml`. The local `main` branch currently has no configured upstream, so this README does not claim that the latest commits are deployed.

## Components

- Source registry and immutable acquisition: [config/sources.json](./config/sources.json), [src/nationality_crime_atlas/acquisition.py](./src/nationality_crime_atlas/acquisition.py)
- Raw / processed data policy: [docs/data_management.md](./docs/data_management.md)
- Canonical dimension mapping: [config/dimension_mappings.json](./config/dimension_mappings.json), [src/nationality_crime_atlas/dimensions.py](./src/nationality_crime_atlas/dimensions.py)
- All-resident product: [config/all_resident_context_contracts.json](./config/all_resident_context_contracts.json), [src/nationality_crime_atlas/all_resident_context.py](./src/nationality_crime_atlas/all_resident_context.py)
- Nationwide comparison including Japanese nationality: [config/nationality_comparison_contract.json](./config/nationality_comparison_contract.json), [src/nationality_crime_atlas/nationality_comparison.py](./src/nationality_crime_atlas/nationality_comparison.py)
- Supplementary indicator engine: [config/indicator_contracts.json](./config/indicator_contracts.json), [src/nationality_crime_atlas/indicators.py](./src/nationality_crime_atlas/indicators.py)
- Compact publication bundle: [src/nationality_crime_atlas/compact_export.py](./src/nationality_crime_atlas/compact_export.py), [config/publication](./config/publication)
- Dashboard and publication checks: [web](./web), [.github/workflows/pages.yml](./.github/workflows/pages.yml)
- Living research brief and workflow map: [docs/brief.md](./docs/brief.md), [docs/workflow.md](./docs/workflow.md)

## Data layout

- `data/raw/`: immutable official downloads plus manifests; never overwritten.
- `data/processed/`: reproducible normalized and derived products; large outputs are ignored by Git.
- `data/processed/_catalog/artifacts.{jsonl,csv}`: compact source-to-artifact provenance catalog.
- `output/`: regenerable publication products.
- `web/public/data/`: hash-verified static dashboard bundle.
- `inbox/`: read-only user-provided research inputs awaiting triage.

## Usage

Set up and verify the Python pipeline:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install --no-build-isolation -e '.[dev]'
.venv/bin/python -m pytest --cov=nationality_crime_atlas --cov-report=term-missing
```

Acquire or reuse a registered edition and regenerate products:

```bash
.venv/bin/nca-acquire --source-id S17
.venv/bin/nca-map-dimensions
.venv/bin/nca-build-indicators
.venv/bin/nca-build-all-resident-context
.venv/bin/nca-build-nationality-comparison
.venv/bin/nca-build-compact-export
```

Run and verify the dashboard:

```bash
cd web
npm ci
npm run sync:data
npm run test:coverage
npm run typecheck
npm run lint
npm run format:check
npm run build
npm run dev
```

Then open `http://localhost:3000/`. `npm run sync:data` only accepts the expected schema and writes a hash manifest; CI uses `npm run verify:data` before building.

## Next work

1. Restore the nationwide numerator selector without hiding Japanese rows when a compatible numerator is unavailable.
2. Push the reviewed commits and verify GitHub Pages only when publication is explicitly requested.
3. Backfill multiple years and show trends, small-denominator volatility, reproducibility, and category/schema drift.
4. Add official-catalog discovery and scheduled acquisition with last-known-good publication behavior.
