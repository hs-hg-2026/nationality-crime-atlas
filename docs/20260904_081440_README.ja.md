# 日本の国籍別犯罪統計 atlas

日本の公的な犯罪統計と人口統計を収集し、定義と出典を保持したうえで、件数と再現可能な`公表統計由来の参考比率`を可視化するprojectである。国籍を集団の本質的riskとして扱わない。

[English](./README.md)

## 背景 (Background)

犯罪の分子と人口の分母は異なる行政機関から公表され、地理・時点・population scope・国籍区分が一致しないことが多い。そのcontextなしに国籍labelだけを示すと、事実に結びつかない印象を生みうる。このprojectは、公表された具体値を隠さず、比較不能・非公表の組合せも明示し、因果や価値判断を可視化へ持ち込まない。

## 目的・ゴール / マイルストーン (Goals / Milestones)

1. official primary sourceをinventory化して検証する。
2. immutableなraw artifactを取得し、再現可能なnormalized data productを作る。
3. count、算式、source、mismatch flag、warning、refusalを一緒に公開する。
4. 全住民の地域contextと、別枠の全国・国籍等別比較を可視化する。
5. historical edition、year-over-year stability check、scheduled updateを追加する。

## 手法・アプローチ (Approach)

- primary regional viewの`総人口`は、日本人と外国人を含む日本の全住民を指す。
- secondaryな全国比較は、日本国籍を含む、公表・評価可能な国籍等categoryをすべて扱う。同じcalculated setから高い側5件・低い側5件を対称に示し、26行の全表も表示する。
- 日本人の分子は、刑法犯検挙人員の全体から全外国人を差し引いたproject-derived residual（残差）である。directに公表された日本人値とはせず、`残差参考値`と明示する。
- 地域contextは全住民10万人当たり、全国の国籍等別比較は対応人口1,000人当たりで表示する。raw numerator／denominatorも残す。
- 計算値はすべて`公表統計由来の参考比率`と呼び、officialまたは正確な`犯罪率`とは呼ばない。
- 非公表の`個別国籍 × 都道府県`犯罪分子は推計・按分しない。
- 小さい分母や少数の分子もwarning付きで表示し、silentに除外しない。

常設の解釈方針は [docs/interpretation_note.md](./docs/interpretation_note.md) を参照。

## 現状 (Status)

**現在地: M4の可視化とfresh independent reviewは完了。全国viewの分子selector復旧とlive deployment確認がrelease前の残作業。**

- registry: 7 source series／8 immutable edition。全件がvalidated artifact catalogに存在する。
- 全住民regional context: 186行 — 144 calculated、42 refused。
- 日本人を含む全国・国籍等別比較: 26行 — 22 calculated、4 refused。
- supplementaryなnationality indicator: 290行 — 250 calculated、40 refused。
- dashboard bundle: compact-export schema v3、sanitized source 8件、SHA-256 `b0f950baa23aac85e44d644b4609ec0bd70d0567d307bfaf013377d85e4ee060`。
- 日本人参考行: 分子181,362人、分母120,296,000人、人口1,000人当たり1.5076。分子sourceはS15 − S08、分母sourceはS17。
- local dashboard: desktopと幅390 pxのmobile layoutを目視確認し、全26行、4 refusal、source ID、warningが見えることを確認した。
- verification: Python 116 test、skip 0、coverage 84.12%。web 56 test、branch coverage 80.41%。typecheck、lint、format、data hash verification、production build、最終32-file GitHub Pages artifact verificationもpassした。

GitHub Pages workflowは`.github/workflows/pages.yml`にある。localの`main` branchにはupstreamが設定されていないため、latest commitのdeploy完了は本READMEでは主張しない。

## 構成 (Components)

- source registryとimmutable acquisition: [config/sources.json](./config/sources.json), [src/nationality_crime_atlas/acquisition.py](./src/nationality_crime_atlas/acquisition.py)
- raw／processed data policy: [docs/data_management.md](./docs/data_management.md)
- canonical dimension mapping: [config/dimension_mappings.json](./config/dimension_mappings.json), [src/nationality_crime_atlas/dimensions.py](./src/nationality_crime_atlas/dimensions.py)
- 全住民product: [config/all_resident_context_contracts.json](./config/all_resident_context_contracts.json), [src/nationality_crime_atlas/all_resident_context.py](./src/nationality_crime_atlas/all_resident_context.py)
- 日本人を含む全国比較: [config/nationality_comparison_contract.json](./config/nationality_comparison_contract.json), [src/nationality_crime_atlas/nationality_comparison.py](./src/nationality_crime_atlas/nationality_comparison.py)
- supplementary indicator engine: [config/indicator_contracts.json](./config/indicator_contracts.json), [src/nationality_crime_atlas/indicators.py](./src/nationality_crime_atlas/indicators.py)
- compact publication bundle: [src/nationality_crime_atlas/compact_export.py](./src/nationality_crime_atlas/compact_export.py), [config/publication](./config/publication)
- dashboardとpublication check: [web](./web), [.github/workflows/pages.yml](./.github/workflows/pages.yml)
- living briefとworkflow map: [docs/brief.md](./docs/brief.md), [docs/workflow.md](./docs/workflow.md)

## Data配置 (Data layout)

- `data/raw/`: official downloadとmanifestのimmutable正本。上書きしない。
- `data/processed/`: 再生成可能なnormalized／derived product。大容量outputはGit管理外。
- `data/processed/_catalog/artifacts.{jsonl,csv}`: sourceとlocal artifactのcompactなprovenance catalog。
- `output/`: 再生成可能なpublication product。
- `web/public/data/`: hash検証済みのstatic dashboard bundle。
- `inbox/`: triage前のuser持ち込み資料。read-onlyとして扱う。

## 使い方 / 進め方 (Usage)

Python pipelineをsetup・検証する:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install --no-build-isolation -e '.[dev]'
.venv/bin/python -m pytest --cov=nationality_crime_atlas --cov-report=term-missing
```

registered editionを取得またはreuseし、productを再生成する:

```bash
.venv/bin/nca-acquire --source-id S17
.venv/bin/nca-map-dimensions
.venv/bin/nca-build-indicators
.venv/bin/nca-build-all-resident-context
.venv/bin/nca-build-nationality-comparison
.venv/bin/nca-build-compact-export
```

dashboardを実行・検証する:

```bash
cd web
npm ci
npm run sync:data
npm run test:coverage
npm run typecheck
npm run lint
npm run format:check
npm run build
npm run dev
```

その後`http://localhost:3000/`を開く。`npm run sync:data`は想定schemaだけを受け付け、hash manifestを生成する。CIはbuild前に`npm run verify:data`を実行する。

## 次の作業 (Next work)

1. compatibleな日本人分子がないmetricでも日本rowを消さず、全国viewの分子selectorを復旧する。
2. publicationが明示的に依頼された段階でreview済みcommitをpushし、GitHub Pagesを実地確認する。
3. 複数年をbackfillし、時系列変化、small-denominator volatility、再現性、category／schema driftを表示する。
4. official catalog discoveryとscheduled acquisition、last-known-good publicationを実装する。
