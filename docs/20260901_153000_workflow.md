# workflow — nationality-crime-atlas

工程の時系列マップ（"You are here"）。個々の判断・失敗は `logbook.md` に分離。これは *地図*。

    ┌──────────────────┐
    │ M0 initial framing ✓│
    └─────────┬────────┘
              ▼
    ┌──────────────────┐
    │ M1 source audit ✓│
    └─────────┬────────┘
              ▼
    ┌────────────────────────────┐
    │ M0 decision gate ✓         │
    └─────────┬────────┘
              ▼
    ┌────────────────────────────┐
    │ M2 取得・正規化baseline ✓   │
    │ acquisition + mapping ✓      │
    └─────────┬──────────────────┘
              ▼
    ┌──────────────────────┐
    │ M3 all-resident context contract ◐│  ◀ You are here
    └─────────┬────────────┘
              ▼
    ┌──────────────────┐
    │ M4 GitHub可視化   │
    └─────────┬────────┘
              ▼
    ┌──────────────────┐
    │ M5 定期更新・監視  │
    └──────────────────┘

| Phase | 入力 | 作業 | 出力 |
|---|---|---|---|
| M0 | project目的、用語候補、M1 findings | initial framing後、exact rateを目標にせずsource pairごとの参考比率を確定 | publication policy / indicator contract |
| M1 | source候補 | 一次資料、定義、coverage、更新頻度をaudit | source inventory / availability matrix |
| M0 decision gate | M1 findings、user decision | raw count＋公表統計由来の参考比率、非推計、表示labelを確定 | approved publication rules |
| M2 ✓ | 検証済みsource、Deep Research 2本、registry v2 | HTTPS acquisition、immutable snapshot、quality gate、artifact catalog、current 7 editionのformal ingestion、canonical dimension mapping | raw snapshot / validated JSONL / provenance・quality・run manifest / artifact catalog / mapping audit |
| M3 ◀ | same-year population・crime statistics、indicator設計材料 | nationality indicator baselineとwarning policyは完了。S15 / S16をpinした全住民regional-context productを次に作る | validated nationality indicators / all-resident context / methods note |
| M4 | validated data product | map・filter・注記・source導線を実装 | GitHub上のvisualization |
| M5 | source公開schedule | 定期取得、変更検知、公開gate | 継続更新されるdashboard |

## M2 → M3 current data flow

    config/sources.json v2    config/quality_profiles.json
       series + editions                 │
              │                          │
              └──────────┬───────────────┘
                         ▼
                  nca-acquire --source-id
                         │
                 registered HTTPS URL
                         │
               temporary HTTP download
                         │
            size / format / pinned hash gate
                         │
                         ▼
    data/raw/<series>/<edition>/<timestamp>/
       official artifact + manifest.json
                         │
                         ▼
           source-specific parser → frozen dataclass records
                         │
                         ▼
                 staged normalized.jsonl
                         │
                    validate_jsonl()
          schema / duplicate / count / hash / sum /
             enum / distinct category / anchor checks
                             │
                 ┌───────────┴───────────┐
                 ▼                       ▼
               PASS                    FAIL
                 │                       │
        atomic promotion          no processed promotion
                 │
                 ▼
    data/processed/<series>/<edition>/<timestamp>/
      normalized.jsonl
      artifact.manifest.json
      quality.json
      run.json
                         │
                         ▼
    data/processed/_catalog/artifacts.{jsonl,csv}
                         │
                         ├───────────────┐
                         │               │
                         │   config/dimension_mappings.json
                         │               │
                         └───────┬───────┘
                                 ▼
                        nca-map-dimensions
       exact code/label + authored alias/composite rules
       matched / ambiguous / unmatched (no fuzzy match)
                                 │
                                 ▼
    data/processed/_mappings/<timestamp>_dimension_mapping/
      dimension_mappings.{jsonl,csv} + summary.json
                                 │
          ┌──────────────────────┴──────────────────────┐
          │                                             │
          │                    config/indicator_contracts.json v2
          │                    source pair / formula / scope /
          │                    expected rows / mismatch policy /
          │                    processed-input SHA-256 pins
          │                                             │
          └──────────────────────┬──────────────────────┘
                                 ▼
                       nca-build-indicators
       input pin / same-year / scope / duplicate / crosswalk / denominator gates
                                 │
                                 ▼
    data/processed/_indicators/<timestamp>_indicators/
      indicator_records.{jsonl,csv} + summary.json
      raw numerator/denominator + quotient + provenance + mismatch
                                 │
          config/small_number_sensitivity.json
          candidate thresholds / sensitivity-only policy
                                 │
                                 ▼
                    nca-audit-small-numbers
        input hash / strict threshold / duplicate-view audit
                                 │
                                 ▼
    data/processed/_indicator_sensitivity/<timestamp>_small_number_sensitivity/
      sensitivity_records.{jsonl,csv} + summary.json

主要な所在:

| Role | Function / artifact | Path |
|---|---|---|
| source metadata | registry schema v2（series / editions） | `config/sources.json` |
| registry validation | `load_source_registry` | `src/nationality_crime_atlas/registry.py` |
| HTTPS acquisition | `acquire_source`, `nca-acquire` | `src/nationality_crime_atlas/acquisition.py`, `acquisition_cli.py` |
| artifact inventory | `rebuild_artifact_catalog` | `src/nationality_crime_atlas/catalog.py`, `data/processed/_catalog/` |
| dimension mapping rules | authored alias/composite/non-equivalent rules | `config/dimension_mappings.json` |
| dimension mapping audit | `generate_dimension_mapping_report`, `nca-map-dimensions` | `src/nationality_crime_atlas/dimensions.py`, `data/processed/_mappings/` |
| indicator contracts | source-pair-specific contract schema v2 | `config/indicator_contracts.json` |
| indicator generation | `generate_indicator_report`, `nca-build-indicators` | `src/nationality_crime_atlas/indicators.py`, `indicator_cli.py`, `data/processed/_indicators/` |
| small-number sensitivity | `generate_small_number_sensitivity_report`, `nca-audit-small-numbers` | `config/small_number_sensitivity.json`, `src/nationality_crime_atlas/small_numbers.py`, `small_number_cli.py`, `data/processed/_indicator_sensitivity/` |
| population parser | `parse_population_t1` | `src/nationality_crime_atlas/population.py` |
| national nationality parser | `parse_npa_nationality_totals` | `src/nationality_crime_atlas/npa_nationality.py` |
| prefecture aggregate parser | `parse_npa_prefecture_table13` | `src/nationality_crime_atlas/npa_prefecture.py` |
| all-resident context parsers | `parse_npa_overall_prefecture_crime`, `parse_npa_prefecture_population` | `src/nationality_crime_atlas/npa_all_residents.py` |
| normalized records | five frozen dataclasses | `src/nationality_crime_atlas/models.py` |
| artifact integrity | `detect_file_format`, `sha256_file`, `build_manifest` | `src/nationality_crime_atlas/provenance.py` |
| immutable raw snapshot | `snapshot_artifact`, `nca-snapshot` | `src/nationality_crime_atlas/snapshot.py`, `snapshot_cli.py` |
| quality baseline | source/version-specific profiles | `config/quality_profiles.json` |
| streaming quality gate | `validate_jsonl`, `nca-validate` | `src/nationality_crime_atlas/quality.py`, `quality_cli.py` |
| atomic offline pipeline | `run_offline_pipeline`, `nca-pipeline` | `src/nationality_crime_atlas/pipeline.py`, `pipeline_cli.py` |
| low-level ingestion | `nca-ingest` | `src/nationality_crime_atlas/cli.py` |
| verification | 91 tests / 84.59% total coverage（branch計測有効） | `tests/` |

Current TODO（実行順）:

1. S15 / S16を独立hash pinした`all_resident_regional_context` contractとgeneratorを実装し、認知件数／検挙件数／検挙人員を別metricで出す。
2. `個別国籍 × 都道府県`、日本国籍prefecture分子、offense-scope mismatchをmachine-readable refusalとして固定する。
3. M4のprimary UIを全住民contextにし、absolute count / per-population context、exact / as-published、display scaleのpolicyを確定する。
4. gitignore対象のgenerated dataから、GitHub公開用compact exportまたはCI build artifactを作る。
5. 全国overview・都道府県map・filter・注記・source導線を備えたvisualization MVPを実装する。
6. 表130の刑法犯componentをsource-preservingに抽出後、地域分布を使うindirect O/Eをsecondary sensitivityとして再審査する。
7. M5でnew-edition discovery、year-over-year drift review、schedule、last-success／failure表示を追加する。

2026-09-01に完了したall-resident source baseline subphase:

- projectのprimary comparatorを日本国籍ではなく、日本に居住する全住民と確定した。nationality viewはsecondaryであり、集団の本質や個人riskを示さない。
- 警察庁表3（S15）と表144（S16）のparser、frozen record、pipeline dispatch、low-level CLI、quality profileをtest-firstで追加した。
- official URLからS15 60 row、S16 48 rowを正式取得し、immutable raw / processedへpromotion。duplicate 0、quality error 0、hash／anchor／distinct gateがpassした。
- S15 / S16の47 prefecture labelは完全一致。S15のprefecture sumはnationalの3 metricに一致。S16の1,000-person差はofficialな千人単位丸めとして保持し、補正しない。
- catalog 7 source／913,345 input rowでmappingを再生成し、720 mapping（matched 674／ambiguous 38／unmatched 8）。new 2 sourceはmatched 96／ambiguous 12。
- indicatorを`20260901_133239_indicators`として再生成し、290 record／250 calculated／40 refused。record JSONLはprevious canonical runとbyte-identical。
- source、compatibility、refusal、東京／埼玉のmechanical checkは[all-resident baseline audit](./20260901_133313_all_resident_baseline_audit.md)に固定した。

2026-08-30に完了したacquisition subphase:

- registry v2で4 series／5 immutable editionを登録。
- 2024年末T1を追加し、444,173 record、duplicate 0、quality error 0で検証。
- registered direct URL downloader、pinned hash gate、same-edition reuse、`--refresh` revision checkを実装。
- 2026-08-30時点の5 editionを正式収集し、全件を`data/raw`／`data/processed`へ保存。
- JSONL/CSV artifact catalogを生成し、dataと出典の対応を一覧化。

2026-08-30に完了したcanonical mapping subphase:

- ISA 2024／2025の国籍・地域codeをunionし、既存196 codeのlabel drift 0、2025年追加1 categoryを確認。
- 913,237 normalized rowから612 distinct mappingを生成し、matched 578／ambiguous 26／unmatched 8を明示。
- NPA `中国`、`韓国・朝鮮`、source region totals、警察region／subregionを単一categoryへ潰さず、fuzzy match・推計・按分を禁止。
- mappingがlabel/category crosswalkに限定され、indicatorの統計的compatibilityを保証しないことをschemaとsummaryへ固定。

2026-08-31に完了したindicator baseline subphase:

- 6 conceptual caseを10 publication contractへ展開し、X/Yのexactとas-published crosswalk policyを分離。
- S08=24、S09=25、S02=47というedition-specific expected row gate、population scope、same-year、duplicate、exact completeness、denominator gateをschema v2とtestへ固定。
- processed `normalized.jsonl`の実hashを`run.json.normalized_sha256`とversion-controlled contract pinの両方へ再照合し、validation後の事後改変とcoordinated local edit、negative population cellをpublish前に停止。
- X/Y 2024 × 2024年末T1、Z 2025 × 2025年末T1から290 recordを生成し、250 calculated／40 refused。Zは47都道府県×2 metricを完備。
- raw numerator／denominator、unscaled quotient、provisional display multiplier、source artifact provenance、mismatch flag、refusal reasonを同じdata productから追跡可能にした。
- `calculation_status`と`statistical_compatibility=not_established`を分離し、計算可能でもofficial crime rateを意味しないことをmachine-readableに固定。
- fresh independent reviewはblocking／high／medium finding 0。processed inputとsibling manifestのcoordinated edit gapをclosedし、contract pin更新は新edition／parser／quality changeと一緒にreviewするgovernance boundaryとした。

2026-08-31に完了したsmall-number sensitivity subphase:

- candidate denominator threshold 7個とnumerator threshold 5個を`policy_status=sensitivity_only`で外部化し、latest indicator hashを検証してimmutable JSONL／CSV／summaryを生成する`nca-audit-small-numbers`をtest-firstで実装。
- denominatorでは`<500`／`<1,000`／`<2,000`が同じ1 observation、numerator `<20`は12 unique observation。dual warning unionは20 / 250 calculated record。
- 厚労省、NCHS／CDC、ONSの一次資料を比較し、fixed denominator cutoffだけをofficial ruleとして流用しない。denominator `<1,000`とnumerator `<20`を別のnon-suppressing UI warningにする案をdecision gateへ送った。

2026-08-31に完了したwarning policy / interpretation subphase:

- indicator contract defaultsへ`small_number_warning_policy_version=1`、`policy_status=approved_project_heuristic`、denominator `<1,000`、numerator `<20`、`default_ranking_behavior=exclude_flagged`を追加し、contract間でpolicy driftがあれば停止する。
- canonical outputを`20260831_215800_indicators`として再生成し、row-level `small_number_warning_flags`と`default_ranking_excluded`を追加した。warning countは`small_denominator_base=8`、`sparse_numerator_count=20`、union 20 / 250 calculated record。
- 地域差・国籍差を因果や個人riskとして読まないための常設文書を`docs/interpretation_note.md`へ固定し、READMEとworkflowから導線を張った。
- fresh reviewでsensitivity summaryがmutableなindicator `latest.json`のpath／hashだけを記録していたMedium findingを検出。参照時点のpointer payloadを各run内の`indicator_input_manifest.json`へsnapshotしてhash固定するRED→GREEN testを追加した。
- 修正版run `20260831_225300_small_number_sensitivity`は当時のindicator inputを解決し、旧runと12 threshold summary／119 sensitivity recordが一致。full suiteは82 test、skip 0、branch計測total coverage 84.33%。当時のindicator／sensitivity artifactのhashも一致した。
- post-fix fresh reviewは元のMedium findingをclosedとし、review対象内の残存correctness／provenance／documentation findingは0件だった。

取得・保存・catalogの運用は [data management and acquisition](./data_management.md)、mapping結果と境界は [canonical dimension mapping audit](./20260830_214058_dimension_mapping_audit.md)、indicator contract／run結果は [indicator contract and production-run audit](./20260831_085815_indicator_contract_and_run_audit.md)、small-number根拠とdecision案は [small-number sensitivity audit](./20260831_205122_small_number_sensitivity_audit.md)、provenance fixは [small-number provenance re-audit](./20260831_225300_small_number_sensitivity_provenance_reaudit.md)、承認後の適用状態は [warning-policy audit](./20260831_215800_indicator_warning_policy_audit.md)、常設解釈注記は [interpretation note](./interpretation_note.md) に固定した。調査質問は [Deep Research prompt](./20260830_135535_parallel_deep_research_prompt.md) に切り出し、持ち込み2本の比較・一次検証結果は [Deep Research比較・採否・実装影響](./20260830_183123_deep_research_comparison.md) にまとめた。全年度matrixはcandidateとして保持しており、代表年以外はproduction registryへpinする時点でbinaryを個別確認する。

確定したpublication rule:

- 完全に一致する分子・分母の取得をproject milestoneやM2のblockerにしない。
- raw countに加え、利用可能な公表統計を単純に割った値を`公表統計由来の参考比率`として表示してよい。
- indicatorごとに分子・分母・算式・出典・期間・地理・母集団の不一致を表示し、officialまたは正確な`犯罪率`とは呼ばない。
- canonical dataでは無次元のquotientとraw numerator/denominatorを保持し、人口1,000人当たり／10万人当たり等はdisplay metadataにする。
- exact crosswalkとas-published mismatchを別indicatorにし、後者は明示的なflag付きで許容するが、推計・按分・fuzzy matchはしない。
- 表13の地理は未解決のため`police_reporting_area_unresolved`を保持し、UIでは「都道府県等別（警察統計上の集計区分）」と表示する。
- 存在しない`個別国籍 × 都道府県`分子は推計しない。
- primary regional baselineは日本国籍ではなく、日本に居住する総人口とする。nationality comparisonはsecondary viewに分離する。
- 警察庁照会・都道府県警auditはdata enrichment trackであり、MVPのgateにしない。

詳細な達成条件とサブマイルストーンは [brief.md](./brief.md)、[source availability audit](./20260830_085603_source_availability_audit.md)、[Deep Research比較](./20260830_183123_deep_research_comparison.md) を参照。

（節目でのみ更新。旧版は git 履歴で管理。git 未使用なら docs/YYYYMMDD_HHMMSS_workflow.md へ退避）
